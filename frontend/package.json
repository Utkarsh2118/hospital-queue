{
  "name": "hospital-queue-frontend-vanilla",
  "version": "1.0.0",
  "private": true,
  "description": "Plain HTML/CSS/JS frontend for MediQueue — no build step required.",
  "scripts": {
    "start": "http-server -p 5173 -c-1"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
